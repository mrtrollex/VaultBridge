# Oracle APEX Integration

## REST APIs

Oracle APEX publishes REST endpoints through ORDS. The API accepts JSON, invokes PL/SQL, checks authentication, and returns database results without exposing internal connection credentials.

## Database Sessions

Oracle sessions execute APEX application logic and PL/SQL packages. Monitor blocked sessions, transaction state, connection pools, and errors independently from the REST API gateway.
